<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<!-- 향후 Mapper interface가 정의 될 위치 -->
<mapper namespace="com.multi.bbs.account.AccountMapper">
	<sql id="selectMemberSql">
		SELECT 	
			MNO, ROLE, NAME, EMAIL, PASSWORD, PHONE, STATE, ENROLL_DATE
		FROM MEMBER
	</sql>
	
	<resultMap type="Member" id="memberResultMap">
		<id 	property="mno" 		column="MNO" />
		<result property="role" 	column="ROLE" />
		<result property="name" 	column="NAME" />
		<result property="email" 	column="EMAIL" />
		<result property="password" column="PASSWORD" />
		<result property="phone" 	column="PHONE" />
		<result property="state" 	column="STATE" />
		<result property="enrollDate" column="ENROLL_DATE" />
	</resultMap>
	
	<select id="selectCount" resultType="int">
		SELECT COUNT(*) FROM MEMBER
	</select>
	
	<select id="selectAll" resultMap="memberResultMap">
		<include refid="selectMemberSql"/>
	</select>
	
	<select id="selectMember" parameterType="string" resultMap="memberResultMap">
		<include refid="selectMemberSql"/>
		WHERE ID = #{id} AND STATUS = 'Y'
	</select>	
	
	<insert id="insertMember" parameterType="Member">
		INSERT INTO MEMBER(
			MNO, ROLE, NAME, 	
			EMAIL, PASSWORD, PHONE,
			STATE, ENROLL_DATE
		) VALUES (
			 0, default, #{name},
			 #{email}, #{password}, #{phone}, 
			 #{state}, DEFAULT
		)
	</insert>
	
	
	
</mapper>